const markus = new Markus('view.mark', {
  // menu: Menu,
  // playground: Playground,
  // player: Player
});

markus.load(() => {
  // markus.get('scenes').set('loading');
  //
  // markus.get('resources').load(() => {
  //   markus.get('scenes').set('menu');
  // });
});
